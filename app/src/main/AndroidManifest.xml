<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.supplycrate1">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SupplyCrate1">
        <activity android:name=".mrchQueueDetails"></activity>
        <activity android:name=".mrchOrderDetails" />
        <activity android:name=".MerchLocation" />

        <activity android:name=".OrderDetailsPage" />
        <activity android:name=".ProjectDetailsPage" />
        <activity android:name=".custProductpage" />
        <activity android:name=".CustMerhInfo" />
        <activity android:name=".CustomerDashboard" />
        <activity android:name=".CategoryForm" />
        <activity android:name=".ProductForm" />
        <activity android:name=".RetailerDashboard" />
        <activity android:name=".dashboardopop" />
        <activity android:name=".crate" />
        <activity android:name=".testop" />
        <activity android:name=".opop" />
        <activity android:name=".dashop" />
        <activity android:name=".dashboardop" />
        <activity android:name=".mainretailer3op" />
        <activity android:name=".mainretailer2op" />
        <activity android:name=".mainretailerop" />
        <activity android:name=".createcrate" />
        <activity android:name=".blank" />
        <activity android:name=".temp1" />
        <activity android:name=".dash" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity
            android:name=".form22"
            android:label="@string/title_activity_form22" />
        <activity android:name=".form2" />
        <activity android:name=".form1" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SignUp" />
        <activity android:name=".Login" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>